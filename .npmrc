# Force npm to not use platform-specific binaries
target_platform=
target_arch=
target_libc=

# Use exact versions to avoid platform-specific resolution issues
save-exact=true

# Disable optional dependencies that might be platform-specific
optional=false
